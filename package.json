{
  "name": "cdnplus-proxy",
  "version": "1.0.0",
  "scripts": {
    "start": "npx playwright install --with-deps chromium && node index.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "playwright": "^1.48.0"
  }
}
